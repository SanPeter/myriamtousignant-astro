name: Deployment and tests

on:
  push:
    branches: [main]

  workflow_dispatch:

# Allow this job to clone the repo and create a page deployment
permissions:
  contents: read
  pages: write
  id-token: write
        
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PUBLIC_PREVENTE_START_AT: ${{ vars.PUBLIC_PREVENTE_START_AT }}
      PUBLIC_PREVENTE_END_AT: ${{ vars.PUBLIC_PREVENTE_END_AT }}
      PUBLIC_PREVENTE_SQUARE_URL_LIMITED: ${{ vars.PUBLIC_PREVENTE_SQUARE_URL_LIMITED }}
      PUBLIC_PREVENTE_SQUARE_URL_LUXE: ${{ vars.PUBLIC_PREVENTE_SQUARE_URL_LUXE }}
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v6

      - name: Install, build, and upload your site
        uses: withastro/action@v6
        with:
          node-version: 24

      # - name: Install Playwright Browsers
      #   run: npx playwright install chromium --with-deps

      #- name: Launch site preview
       # run: npm run preview &

      # - name: Run Playwright tests
      #   run: npm run test:e2e
      #   env:
      #     PLAYWRIGHT_TEST_BASE_URL: http://localhost:4321
    
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
